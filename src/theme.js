import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: "#dff1f0"
        },
        secondary: {
            main: "#0f0a0e"
        }
    },
    background: {
        default: "#13110c"
    }
});

export default theme;